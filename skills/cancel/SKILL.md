---
description: Cancel comment mode and discard any queued highlight comments
disable-model-invocation: true
---

Run `highlight-state mode-off` and `highlight-state clear` using the Bash tool.

Confirm to the user in one short sentence that comment mode was cancelled and the queue was cleared.
